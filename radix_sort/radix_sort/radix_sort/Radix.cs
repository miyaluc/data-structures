using System;
using System.Collections.Generic;
using System.Text;

namespace radix_sort
{
    class Radix
    {
        public void RadixCount(int n)
        {
            int[] array = new int[12];
            for (int i = 0; i < array.Length; i++)
            {
                int p = 10;

                for(int x = 0; x < array.Length;  )
                {
                    int n mod p;
                }
            }
        }
    }
}
